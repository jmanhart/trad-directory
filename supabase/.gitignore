# Supabase
.branches
.temp

# dotenvx
.env.keys
.env.local
.env.*.local

# Local seed data — regenerate with: npm run db:seed:pull
seed.sql

# Dashboard SQL snippets synced by the CLI — not tracked
snippets/
